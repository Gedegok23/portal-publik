# Leave unset to run against built-in mock data (lib/mock-data.ts).
# Set this once the Golang backend is deployed to switch to live data —
# no page or component code needs to change.
NEXT_PUBLIC_API_BASE_URL=
